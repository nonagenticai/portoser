# requirements.txt is provided as a fallback for tooling that doesn't
# support PEP 621 / pyproject.toml. The authoritative dependency list
# lives in pyproject.toml (and uv.lock for reproducible builds).
# Keep in sync if you edit either file.

fastapi>=0.117.0
uvicorn[standard]>=0.34.0
pydantic>=2.11.0,<3.0
pydantic-settings>=2.7,<3.0
pyyaml>=6.0.2
websockets>=15.0
python-multipart>=0.0.18

# Authentication & Secrets
python-dotenv>=1.0.1
hvac>=2.4.0
python-jose[cryptography]>=3.5.0
python-keycloak>=5.1.1
cryptography>=46.0.0
bcrypt>=4.3.0

# MCP (Model Context Protocol)
fastmcp>=2.12.0,<4.0.0
mcp>=1.13.0,<2.0.0
RestrictedPython>=8.0,<9.0

# Database
asyncpg>=0.30.0
psycopg2-binary>=2.9.10
sqlalchemy>=2.0.45

# JWT & Auth
pyjwt>=2.10.0,<3.0.0
passlib[bcrypt]>=1.7.4,<2.0.0

# Utilities
uuid7>=0.1.0
httpx>=0.28.1
sse-starlette>=2.3.4,<4.0.0
redis>=6.0.0,<8.0.0
typer>=0.15.0
rich>=14.0.0

# Rate limiting
slowapi>=0.1.9

# Monitoring
prometheus-client>=0.22.0
