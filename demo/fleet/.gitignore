# Generated keypair — regenerated on first fleet-up.sh.
keys/

# Seeded portoser-home (deployments/, knowledge/, metrics_snapshots/) —
# regenerated by fleet-up.sh's seed-data.py step on every run.
state/
